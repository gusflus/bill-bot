{
  "name": "bill-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Watches Gmail for utility bill emails, splits each bill among roommates by weight, and texts everyone their share. Infrastructure and bill processing are Python on AWS (CDK + Lambda + Bedrock); the Gmail watcher is Apps Script because the platform requires JS.",
  "scripts": {
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "diff": "cdk diff",
    "push": "clasp push"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "aws-cdk": "^2.1138.0"
  }
}
