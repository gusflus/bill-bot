{
  "name": "bill-bot",
  "version": "0.2.0",
  "private": true,
  "description": "A single Google Apps Script project that watches Gmail for utility bill emails, splits each bill among roommates by configurable weight, and posts a Discord notification with per-roommate Venmo links. Payment tracking and dedup live in an auto-created Google Sheet.",
  "scripts": {
    "push": "clasp push",
    "test": "node --test tests/*.test.js",
    "preview": "node dev/preview.js"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
